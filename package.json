{
  "name": "navmash-workspace",
  "private": true,
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "start": "npm --prefix backend start & npm --prefix frontend start & wait"
  }
}
